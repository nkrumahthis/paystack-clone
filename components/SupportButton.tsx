import React from 'react'

function SupportButton() {
    return (
        <button className="absolute bottom-0 right-0 bg-gray-900 text-gray-300 hover:text-white p-3 rounded-full text-sm m-4">
            ? SUPPORT
        </button>
    )
}

export default SupportButton